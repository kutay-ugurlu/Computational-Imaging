function res = forward_A(H,lambda,D1,D2,p)
H_flipped = flip(flip(H,1),2);
D1_flipped = flip(D1);
D2
Cp = conv(p,H,'same');
CtranposeCp = conv(Cp,H_flipped,'same');
R1 = conv2(conv2(p,D1,'same'),'same');
Dp2 = conv2(p,D2,'same');



end